#What day is it tomorrow function

month = int(input("Month: "))
date = int(input("Date: "))

def tomorrow(month, date):
    if month == 1 and date <= 31:
        ndate = date + 1
        if date == 31:
            print("new month" + ndate)
        else:
            print("same month" + ndate)

   if month == 2 and date <= 31:
        ndate = date + 1
        if date == 31:
            print("new month" + ndate)
        else:
            print("same month" + ndate)

    if month == 3 and date <= 31:
        ndate = date + 1
        if date == 31:
            print("new month" + ndate)
        else:
            print("same month" + ndate)

    if month == 4 and date <= 31:
        ndate = date + 1
        if date == 31:
            print("new month" + ndate)
        else:
            print("same month" + ndate)

    if month == 5 and date <= 31:
        ndate = date + 1
        if date == 31:
            print("new month" + ndate)
        else:
            print("same month" + ndate)

    if month == 6 and date <= 31:
        ndate = date + 1
        if date == 31:
            print("new month" + ndate)
        else:
            print("same month" + ndate)